# 第 2 章导读:分词器 —— 从文本到 token id

> 这是 MiniMind 教程的第 2 章。完整的交互版见 [`ch02.ipynb`](./ch02.ipynb)。

## 章节摘要

本章从零拆解分词器(tokenizer)的完整原理。我们从 BPE 算法的核心思想 ——「按频率合并高频相邻字节对」—— 开始,手动实现一个极简 BPE 来理解 merge rule 的工作方式。然后引入 ByteLevel 预分词:为什么以字节(而非字符)为初始字母表能彻底消灭 unknown token。接着分析 minimind 的实际参数:vocab=6400 的极小词表如何将 embedding 层参数控制在 4.9M,36 个 special token(含 ChatML 边界、tool_call、think)如何保证对话格式的原子性,以及 Jinja2 chat_template 如何把消息列表渲染成 `<|im_start|>role\n...<|im_end|>` 格式。最后用压缩率测试量化中文(~1.6 chars/token)与英文(~3.9 chars/token)的效率差异。

## 学习目标

读完本章后,你应该能:

- 解释 BPE 算法的训练(频率统计 + 合并)和编码(按 merge rule 优先级合并)过程
- 说明 ByteLevel 为什么能保证「任何文本都不会产生 `<unk>`」
- 计算 vocab=6400 对 embedding 层参数量的影响(4.9M vs 32000 词表的 24.6M)
- 列出 minimind 的 36 个 special token 及其分组(系统级 / 功能级 / buffer)
- 解释 special token 的三个保证:原子编码、稳定信号、往返一致
- 用 `apply_chat_template` 渲染一段对话,并理解 ChatML 格式
- 估算中英文混合文本的 token 数并理解压缩率的工程影响

## 关键 minimind 文件引用

本章涉及的核心代码:

| 文件 | 行号 | 作用 |
|---|---|---|
| `trainer/train_tokenizer.py` | 9 | `VOCAB_SIZE = 6400` —— 词表大小定义 |
| `trainer/train_tokenizer.py` | 10 | `SPECIAL_TOKENS_NUM = 36` —— 特殊 token 数量 |
| `trainer/train_tokenizer.py` | 25 | `models.BPE()` —— BPE 模型初始化 |
| `trainer/train_tokenizer.py` | 26 | `pre_tokenizers.ByteLevel(add_prefix_space=False)` —— ByteLevel 预分词 |
| `trainer/train_tokenizer.py` | 28-33 | 系统级 special tokens(ChatML / 多模态 / 语音) |
| `trainer/train_tokenizer.py` | 35-39 | 功能级 tokens(tool_call / tool_response / think) |
| `trainer/train_tokenizer.py` | 40-42 | buffer tokens(预留扩展位) |
| `trainer/train_tokenizer.py` | 43-48 | `BpeTrainer` 配置(vocab_size + initial_alphabet + special_tokens) |
| `trainer/train_tokenizer.py` | 51 | `decoders.ByteLevel()` —— ByteLevel 解码器(保证往返一致) |
| `trainer/train_tokenizer.py` | 84-86 | bos/eos/pad 定义(`<\|im_start\|>` / `<\|im_end\|>` / `<\|endoftext\|>`) |
| `trainer/train_tokenizer.py` | 100 | `chat_template` —— Jinja2 模板(ChatML + tools + think) |
| `trainer/train_tokenizer.py` | 108-165 | `eval_tokenizer()` —— 压缩率测试与流式解码 |
| `model/tokenizer_config.json` | 1-20 | added_tokens decoder(special token 元信息) |
| `model/tokenizer.json` | — | BPE 词表 + merge rules(完整训练产物) |

## 术语表

| 术语 | 英文 | 解释 |
|---|---|---|
| 分词器 | Tokenizer | 将文本转换为 token id 序列的组件 |
| BPE | Byte Pair Encoding | 按频率合并高频相邻字节对的子词算法 |
| 子词 | Subword | 介于字符和词之间的单位,BPE 的学习产物 |
| 合并规则 | Merge Rule | BPE 训练记录的合并操作(按优先级排序) |
| 字节级预分词 | ByteLevel Pre-tokenization | 以 UTF-8 字节为初始字母表,保证无 unknown |
| 词表大小 | vocab_size | 词表中 token 的总数,minimind = 6400 |
| 特殊 token | Special Token | 注册为不可分割的原子 token,不被 BPE 拆分 |
| 压缩率 | Compression Ratio | 字符数 / token 数,衡量 tokenizer 效率 |
| 对话模板 | chat_template | Jinja2 模板,将消息列表渲染为模型输入格式 |
| ChatML | Chat Markup Language | `<\|im_start\|>role\n...<\|im_end\|>` 对话格式协议 |
| 往返一致 | Round-trip Consistency | decode(encode(text)) == text |
| 字节 | Byte | 8 位整数(0-255),所有文本的终极表示 |
| 序列开始 | BOS (Beginning of Sequence) | minimind 中为 `<\|im_start\|>` |
| 序列结束 | EOS (End of Sequence) | minimind 中为 `<\|im_end\|>` |

## 预计学习时长

- **30-45 分钟**(含运行 BPE 演示代码)
- **前置知识**:基础 Python;理解字典和频率统计;读完了第 1 章
- **前置章节**:[第 1 章](../ch01/01_main-chapter-code/README.md)

## 核心认知

> 分词器是模型的**输入边界**。BPE + ByteLevel 的组合让 minimind 用 6400 个 token 就能表示世界上任何文本 —— 代是中文压缩率偏低。36 个 special token 和 chat_template 共同定义了模型与外界交互的「语法协议」。
